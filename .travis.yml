before_install:
    - sudo apt-get -qq update
install:
    - sudo apt-get install -y libxml2-dev autoconf automake autopoint bash bison bzip2 flex gettext git g++ gperf intltool libffi-dev libgdk-pixbuf2.0-dev libtool libltdl-dev libssl-dev libxml-parser-perl make openssl p7zip-full patch perl pkg-config python ruby scons sed unzip wget xz-utils g++-multilib libc6-dev-i386
before_script:
    - git clone https://github.com/mxe/mxe.git
    - cd mxe
    - MXE_TARGETS='x86_64-w64-mingw32.static i686-w64-mingw32.static' make gcc
script:
    ./DLL\ Solution/build.sh
notifications:
    email: false
