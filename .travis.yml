before_install:
    - sudo apt-get -qq update
install:
    - sudo apt-get install gcc-mingw-w64-i686 g++-mingw-w64-i686 binutils-mingw-w64-i686 mingw-w64-dev
script:
    ./DLL\ Solution/build.sh
notifications:
    email: false
